<div class="container-fluid">
  <div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">
      Biên tập & Duyệt: <span class="text-primary font-weight-bold"><%= @story["kanji_text"] %></span>
    </h1>
    <%= link_to admin_stories_path, class: "btn btn-secondary btn-sm shadow-sm" do %>
      <i class="fas fa-arrow-left"></i> Quay lại danh sách
    <% end %>
  </div>

  <div class="row">
    <div class="col-md-4">
      <div class="card shadow mb-4">
        <div class="card-header py-3 bg-info text-white">
          <h6 class="m-0 font-weight-bold"><i class="fas fa-user-edit mr-2"></i>Nội dung User đóng góp</h6>
        </div>
        <div class="card-body">
          <div class="mb-3">
            <label class="small text-muted font-weight-bold">NGƯỜI TẠO:</label>
            <div class="font-weight-bold"><%= @story["user_email"] || "Ẩn danh" %></div>
          </div>

          <div class="mb-3">
            <label class="small text-muted font-weight-bold">CHỮ KANJI:</label>
            <div class="display-4 text-center border rounded bg-light py-2"><%= @story["kanji_text"] %></div>
          </div>

          <div class="mb-3">
            <label class="small text-muted font-weight-bold">CÂU CHUYỆN GỐC:</label>
            <div class="p-3 bg-light border rounded text-dark" style="min-height: 100px; font-size: 1rem;">
              <%= @story["kanji_story"] %>
            </div>
          </div>

          <% if @story["kanji_description"].present? %>
            <div class="mb-3">
              <label class="small text-muted font-weight-bold">GHI CHÚ THÊM:</label>
              <div class="small font-italic"><%= @story["kanji_description"] %></div>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-md-8">
      <div class="card shadow mb-4">
        <div class="card-header py-3 bg-primary text-white">
          <h6 class="m-0 font-weight-bold"><i class="fas fa-check-circle mr-2"></i>Kiểm tra & Phê duyệt</h6>
        </div>
        <div class="card-body">

          <% if @story["kanji_id"].present? %>
            <div class="alert alert-warning border-left-warning shadow-sm mb-4" role="alert">
              <div class="d-flex align-items-center">
                <i class="fas fa-exclamation-triangle fa-2x mr-3"></i>
                <div>
                  <h5 class="alert-heading font-weight-bold mb-1">Chữ này đã có trong hệ thống!</h5>
                  <p class="mb-0">
                    Nội dung này sẽ được gộp vào chữ <strong class="text-dark"><%= @story["kanji_text"] %></strong> đã tồn tại.
                  </p>
                </div>
              </div>
            </div>
          <% end %>

          <%= form_with url: approve_admin_story_path(@story["id"]), method: :patch, local: true do |f| %>

            <%= f.hidden_field :kanji, value: @story["kanji_text"] %>

            <h5 class="text-gray-800 font-weight-bold border-bottom pb-2 mb-3">1. Thông tin cơ bản</h5>
            <div class="row">
              <div class="col-md-6 form-group">
                <label>Hán Việt <span class="text-danger">*</span></label>
                <%= f.text_field :translation, class: "form-control", value: @story["translation"], required: true %>
              </div>
              <div class="col-md-6 form-group">
                <label>Nghĩa Tiếng Việt <span class="text-danger">*</span></label>
                <%= f.text_field :meaning, class: "form-control", value: @story["meaning"], required: true %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-6 form-group">
                <label>Onyomi (Âm On)</label>
                <%= f.text_field :onyomi, class: "form-control", value: @story["on_pronunciation"] || @story["onyomi"], placeholder: "VD: JIN, NIN" %>
              </div>
              <div class="col-md-6 form-group">
                <label>Kunyomi (Âm Kun)</label>
                <%= f.text_field :kunyomi, class: "form-control", value: @story["kun_pronunciation"] || @story["kunyomi"], placeholder: "VD: hito" %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-6 form-group">
                <label>Số nét</label>
                <%= f.number_field :stroke_count, class: "form-control", value: @story["num_strokes"], placeholder: "VD: 2" %>
              </div>
              <div class="col-md-6 form-group">
                <label>Cấp độ JLPT</label>
                <%= f.select :jlpt_level, options_for_select((1..5).to_a.reverse.map { |n| ["N#{n}", n] }, @story["jlpt"] || 5), {}, { class: "form-control custom-select" } %>
              </div>
            </div>

            <h5 class="text-gray-800 font-weight-bold border-bottom pb-2 mb-3 mt-3">2. Cấu tạo & Hình ảnh</h5>
            <div class="row">
              <div class="col-md-6 form-group">
                <label>Bộ thủ (Radical)</label>
                <%= f.text_field :radical, class: "form-control", value: @story["radical"], placeholder: "VD: Nhân (人)" %>
              </div>
              <div class="col-md-6 form-group">
                <label>Thành phần (Components)</label>
                <%= f.text_field :components, class: "form-control", value: @story["components"], placeholder: "Các bộ phận cấu thành" %>
              </div>
            </div>
            <div class="form-group">
              <label>Link ảnh cách viết (GIF/JPG)</label>
              <%= f.text_field :writing_image_url, class: "form-control", value: @story["writing_image_url"], placeholder: "https://example.com/kanji.gif" %>
            </div>

            <h5 class="text-gray-800 font-weight-bold border-bottom pb-2 mb-3 mt-3">3. Câu chuyện & Mở rộng</h5>

            <div class="form-group">
              <label class="font-weight-bold text-success">Câu chuyện / Mẹo nhớ (Story)</label>
              <%= f.text_area :kanji_description, class: "form-control", rows: 3, value: @story["kanji_story"] || @story["kanji_description"] %>
              <small class="form-text text-muted">Bạn có thể chỉnh sửa lại câu chuyện của user cho hay hơn trước khi duyệt.</small>
            </div>

            <div class="row">
              <div class="col-md-6 form-group">
                <label>Từ vựng liên quan</label>
                <%= f.text_area :vocabulary, class: "form-control", rows: 3, value: @story["vocabulary"], placeholder: "VD: 学生 (Gakusei): Học sinh..." %>
              </div>
              <div class="col-md-6 form-group">
                <label>Ví dụ câu</label>
                <%= f.text_area :examples, class: "form-control", rows: 3, value: @story["examples"], placeholder: "VD: 日本語を勉強します..." %>
              </div>
            </div>

            <div class="alert alert-info mt-3">
              <i class="fas fa-info-circle mr-1"></i>
              Nhấn <strong>"Xác nhận duyệt"</strong> sẽ cập nhật toàn bộ thông tin trên vào hệ thống Java và công khai bài viết.
            </div>

            <button type="submit" class="btn btn-success btn-lg btn-block font-weight-bold shadow mt-4">
              <i class="fas fa-check mr-2"></i> XÁC NHẬN DUYỆT & ĐĂNG BÀI
            </button>
          <% end %>

          <hr class="my-4">

          <div class="bg-light p-3 rounded border">
            <h6 class="text-danger font-weight-bold mb-3"><i class="fas fa-times-circle mr-1"></i>Từ chối đóng góp này</h6>
            <%= form_with url: reject_admin_story_path(@story["id"]), method: :patch, local: true, class: "form-inline" do |f| %>
              <div class="input-group w-100">
                <%= f.text_field :reason, class: "form-control", placeholder: "Nhập lý do từ chối...", required: true %>
                <div class="input-group-append">
                  <button type="submit" class="btn btn-danger font-weight-bold">
                    Xác nhận Từ chối
                  </button>
                </div>
              </div>
            <% end %>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>